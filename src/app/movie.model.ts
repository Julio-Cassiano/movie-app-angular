export type MovieModel = {
    id: number,
    name: string,
    synopsis: string,
    release_date: string,
    duration_in_seconds: number,
    image_path: string,
    username: string,
    director_names: string[]
}